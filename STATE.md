# GuPiao Loop 状态

最后更新：2026-07-11

## 当前目标

保持模拟盘自动交易链路稳定运行。在不接触实盘、不输出密钥、保持 `LIVE_TRADING_ENABLED=false` 与 `BROKER_ADAPTER=simulation` 的前提下，让后端、前端、worker 和 scheduler 持续运行，并等待下一个 A 股交易窗口自动执行一夜持股法。

## 本轮结果

- 初始基线：后端 `58 passed / 3 failed`，scheduler 因交易日历提供方全部失败而退出。
- 当前后端：Python 3.12.12 下全套测试 `103 passed`；Ruff 与 6 个关键验收脚本全部通过。
- 当前前端：Node 20 下生产构建通过，共转换 1560 个模块。
- scheduler：计划使用数据库条件更新进行原子占用，并带 45 秒可恢复租约；瞬态数据失败按 15 秒退避，崩溃后会对账已有运行，避免重复下单。入场只允许 14:40 后 59 秒内执行，退出可在 09:35 至 10:00 内恢复执行。
- 一键启动：幂等准备 10 万模拟账户、观察股票、一夜持股策略和两个启用计划；要求环境保持模拟模式，并主动关闭数据库 LIVE 标志、LIVE 账户和 LIVE 计划；管理员密码会与当前安全配置同步，启动失败会清理本轮进程。
- 持续运行：macOS 用户级 LaunchAgent `com.gupiao.simulation-loop` 已启用，外层 `caffeinate` 防止空闲睡眠，四个子进程异常退出时由 launchd 重启。
- 数据真实性：AKShare 已同步 5530 只股票主数据；mootdx 实际刷新 5203 只沪深行情；AKShare 公告源实际保存 9059 条记录。观察版行情超过 60 秒、公司事件超过 1800 秒、时间在未来或来源不健康时均禁止下单。
- 自动窗口：公告在交易日 14:20-14:35 每 300 秒同步；全市场行情在 14:35-14:41 刷新；09:30-10:00 只刷新实际模拟持仓，避免全市场轮询拖慢退出。通知发送在交易数据窗口外处理。
- 模拟交易：一夜持股法模拟配置已启用，入场计划 14:40、退出计划 09:35；端到端测试证明到点只创建一笔模拟订单，同一窗口不重复，T+1 次交易日释放后才可卖出，不同策略配置只卖出自身历史成交归属的仓位。
- 数据库：本地 SQLite 已启用 WAL、30 秒 busy timeout 和旧表 `next_run_at` 自动迁移；worker 在提交失败后先 rollback，主循环捕获异常后继续运行。
- LIVE 硬门：API 写入口和最终下单函数都要求环境层两把开关同时开放；当前运行配置关闭，实际调用启用 LIVE 接口返回 HTTP 403，数据库 LIVE 关闭、紧急停止开启、启用账户/计划/真实订单均为 0。
- 60m 回测：`000001.SZ`，2026-06-24 至 2026-06-25，`timeframe_used=60m`，`data_source=cache`，尾盘最后一根 K 线入场，净收益 `-24.1861`，收益率 `-0.2419%`，退出码 0。
- 安全状态：数据库 LIVE 关闭，真实订单数为 0，运行配置保持 `LIVE_TRADING_ENABLED=false`、`BROKER_ADAPTER=simulation`。
- 当前时间为 2026-07-11 周六，服务已稳定运行到用户要求的次日；周末不会生成合法模拟订单。下一次可能入场时间是 2026-07-13 周一 14:40；届时仍须有新鲜行情、公告和通过策略/风控的候选股票。
- 当前限制：换手率、VWAP、MA5 与真实上市日期仍是 v1 占位字段；当前自动选股主要使用真实价格、涨跌幅、成交额、交易状态和公告风险。后续应作为独立增量 spec 补齐，不应把当前结果解读为完整因子模型。

## 高优先级

1. 保持 LaunchAgent 与电脑供电/联网，等待 2026-07-13 14:40 的首个合法入场窗口。
2. 周一检查 `backend/.scheduler.log`、策略最近运行、模拟订单与持仓；外部行情不可用时允许失败关闭，禁止用演示数据替代。
3. 为换手率、VWAP、MA5、上市日期和基准过滤补充独立中文增量 spec。
4. 在任何实盘工作开始前重新执行 fail-closed 风控审查并取得人工确认。

## 待验收清单

- [x] 后端健康检查成功。
- [x] 前端入口返回 HTTP 200。
- [x] worker 与 scheduler 均至少连续存活 60 秒。
- [x] 交易日历数据源失败后，模拟盘 scheduler 仍存活；LIVE 配置 fail-closed。
- [x] 使用 `--preferred-timeframe 60m` 的回测成功并保留结果证据。
- [x] 后端全套 `103 passed`、Ruff 与 scheduler 原子占用/崩溃恢复端到端测试通过。
- [x] 前端 Node 20 构建通过。
- [x] 运行时验证 `LIVE_TRADING_ENABLED=false`。
- [x] 运行时验证 `BROKER_ADAPTER=simulation`。
- [x] 真实数据探测成功：5203 只行情、9059 条公告记录，两个来源均健康。
- [x] 用户级 LaunchAgent 与 `caffeinate` 生效，四进程连续运行超过 60 秒。
- [x] 登录后核心 API 全部 HTTP 200；启用 LIVE 请求 HTTP 403。
- [x] 独立代码审查的 8 项问题均已落实修复或测试覆盖。
- [x] 所有修改均位于专用 worktree，未 merge、未启用实盘、未发送真实订单。

## 观察项

- 外部行情、公告与交易日历提供方可能不稳定；验收已覆盖失败降级，到点仍可能因外部源不可用而不下单。
- 周末不会交易；服务运行不等于保证产生订单，策略没有候选或风控拦截同样是正确结果。
- 60m 回测结果为单次历史结果，不构成收益承诺。
- 外部行情或公司事件数据恢复前，策略不会创建模拟订单；这属于数据风控失败关闭，不是进程故障。

## 已知环境事项

- 原 Conda Python 3.12.3 的 `readline` 扩展会段错误；本轮改用 uv Python 3.12.12 验证。
- 系统 Homebrew Node 16 缺少 ICU；启动脚本优先选择本机 NVM Node 20。
- 第一周 Loop 仍为 report-only；未创建 Automation。

---

运行记录：见 `loop-run-log.md`。
